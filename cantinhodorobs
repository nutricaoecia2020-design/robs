<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cantinho do Robs | Pedido Online</title>
    <!-- Adicione aqui os mesmos links de fontes e ícones do seu projeto original -->
    <style>
        /* CSS Integrado: Base Dark, Grid de Produtos, Tabela de Busca e Gestão */
        :root { --primary: #22c55e; --accent-gold: #f59e0b; --bg-dark: #0b0b0b; }
        body { background: var(--bg-dark); color: white; font-family: 'Inter', sans-serif; margin: 0; }
        
        /* Layout de Navegação/Tabs */
        .tab-bar { display: flex; gap: 10px; padding: 20px; justify-content: center; }
        .tab-btn { background: #333; color: white; border: none; padding: 10px 20px; border-radius: 20px; cursor: pointer; }
        .tab-btn.active { background: var(--primary); }
        
        /* Ocultar seções inativas */
        .section { display: none; padding: 20px; max-width: 1000px; margin: auto; }
        .section.active { display: block; }

        /* Estilização da Busca (do seu primeiro index) */
        #filtro { width: 100%; padding: 15px; margin-bottom: 20px; border-radius: 10px; background: #262626; border: 1px solid #444; color: white; }
    </style>
</head>
<body>

    <!-- Navegação Superior -->
    <div class="tab-bar">
        <button class="tab-btn active" onclick="showSection('menu')">Cardápio</button>
        <button class="tab-btn" onclick="showSection('busca')">Busca Rápida</button>
        <button class="tab-btn" id="admin-tab" onclick="showSection('gestao')" style="display:none;">Painel Admin</button>
    </div>

    <!-- SEÇÃO: CARDÁPIO (Interface de Vendas) -->
    <section id="menu" class="section active">
        <!-- Banner e Grid de Produtos do seu segundo index -->
        <div id="grid-produtos" class="grid-produtos"></div>
    </section>

    <!-- SEÇÃO: BUSCA (Otimizada do seu primeiro index) -->
    <section id="busca" class="section">
        <input type="text" id="filtro" onkeyup="filtrarTabela()" placeholder="Buscar por sabor ou ingrediente...">
        <table id="tabelaPizzas" style="width:100%; border-collapse: collapse; background: #181818;">
            <!-- Conteúdo da tabela do seu primeiro index -->
        </table>
    </section>

    <!-- SEÇÃO: GESTÃO (Seu terceiro index integrado) -->
    <section id="gestao" class="section">
        <h2>Painel de Gestão - DeliGestor Pro</h2>
        <!-- Controles do seu terceiro index -->
    </section>

<script>
    // Lógica Unificada
    function showSection(id) {
        document.querySelectorAll('.section').forEach(s => s.classList.remove('active'));
        document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'));
        document.getElementById(id).classList.add('active');
        event.currentTarget.classList.add('active');
    }

    // Gatilho de segurança: Mostrar painel admin apenas se clicar no logo ou atalho
    // Exemplo: document.getElementById('admin-tab').style.display = 'block';

    // ... (Aqui você insere todas as funções JavaScript originais mescladas)
    
    // Filtro do Index 1
    function filtrarTabela() { /* Sua lógica original */ }

    // Carrinho e API do Index 2
    // Painel de pedidos do Index 3
</script>
</body>
</html>
